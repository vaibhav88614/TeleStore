# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

# LOCAL VENDOR NOTE
# =================
# This is upstream `quick-xml 0.41.0` verbatim, with ONLY the `version` field
# below relabelled from "0.41.0" to "0.39.99". The relabel is required because
# `plist 1.9.0` and `wayland-scanner 0.31.10` (transitive deps via tauri and
# the wayland clipboard stack) both request `quick-xml = "^0.39"`, which
# excludes 0.40+ under Cargo's 0.x semver rules. Upstream `plist` master still
# pins 0.39.2, so there is no released consumer of the fixed version yet.
#
# Bumping the source to 0.41.0 code carries the fixes for:
#   - RUSTSEC-2026-0194 (quick-xml: O(N^2) duplicate-attribute-check CPU DoS)
#   - RUSTSEC-2026-0195 (quick-xml: unbounded NsReader namespace-declaration allocation)
# Both fixes are purely additive (new enum variant, new methods, new pre-filter);
# neither `plist` nor `wayland-scanner` references the added surface, so this
# is a drop-in security bump.
#
# Wired in via `[patch.crates-io] quick-xml = { path = "vendor/quick-xml" }`
# in `app/src-tauri/Cargo.toml`. Revisit and remove once `plist` and
# `wayland-scanner` publish releases that depend on quick-xml >= 0.41.

[package]
edition = "2021"
rust-version = "1.79"
name = "quick-xml"
version = "0.39.99"
build = false
include = [
    "src/*",
    "LICENSE-MIT.md",
    "README.md",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "High performance xml reader and writer"
documentation = "https://docs.rs/quick-xml"
readme = "README.md"
keywords = [
    "xml",
    "serde",
    "parser",
    "writer",
    "html",
]
categories = [
    "asynchronous",
    "encoding",
    "parsing",
    "parser-implementations",
]
license = "MIT"
repository = "https://github.com/tafia/quick-xml"

[package.metadata.docs.rs]
all-features = true

[features]
async-tokio = ["tokio"]
default = []
encoding = ["encoding_rs"]
escape-html = []
overlapped-lists = []
serde-types = ["serde/derive"]
serialize = ["serde"]

[lib]
name = "quick_xml"
path = "src/lib.rs"
bench = false

[dependencies.arbitrary]
version = "1"
features = ["derive"]
optional = true

[dependencies.document-features]
version = "0.2"
optional = true

[dependencies.encoding_rs]
version = "0.8"
optional = true

[dependencies.memchr]
version = "2.1"

[dependencies.serde]
version = ">=1.0.180"
optional = true

[dependencies.tokio]
version = "1.10"
features = ["io-util"]
optional = true
default-features = false

[dev-dependencies.criterion]
version = ">=0.4,<0.9"

[dev-dependencies.pretty_assertions]
version = "1.4"

[dev-dependencies.regex]
version = "1"

[dev-dependencies.serde-value]
version = "0.7"

[dev-dependencies.serde_derive]
version = "1.0.206"

[dev-dependencies.tokio]
version = "1.21"
features = [
    "macros",
    "rt",
]
default-features = false

[dev-dependencies.tokio-test]
version = "0.4"

[profile.bench]
lto = true
debug = 2
